import { _decorator, Component, Node, Prefab, instantiate } from 'cc';
const { ccclass, property } = _decorator;

/**
 * 单词预制体管理器
 * 用于在 Cocos Creator 编辑器中创建预制体
 */
@ccclass('WordPrefabScript')
export class WordPrefabScript extends Component {
    start() {
        // 预制体初始化逻辑
    }
}
